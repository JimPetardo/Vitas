using UnityEngine;
using System.Collections;

public class collisionForStat : MonoBehaviour {

	void OnCollisionStay(Collision cubo) {
		print ("dentro");
		/*
	
		if (renderer.material.color != Color.red) {
			punti++;
			audio.PlayOneShot (ButtonSound);
		}

		if (mano.collider.gameObject.name == "bone3")
			manoToccata = "0";
		
		if (mano.collider.gameObject.name == "destra")
			manoToccata = "1";

		frase2 = prove + "\t1\t" + (-timeLeftRecived + 181 ).ToString("F1") + "\t" + this.transform.position.x.ToString("F4") + "\t" + this.transform.position.z.ToString("F4") + "\t" + manoToccata;

		arrayStrighe [prove] = frase2;
		renderer.material.color = Color.red;
		*/
		
	}
}